#ifndef MAIN_H
#define MAIN_H

/* standard libraries it nessary in work */
#include <stdio.h>
#include <unistd.h>
#include <string.h>
#include <stdlib.h>


/* prototype my functions */
void _puts_recursion(char *s);
void _putchar(char c);
void _print_rev_recursion(char *s);
int _strlen_recursion(char *s);
int factorial(int n);




#endif
