#include "variadic_functions.h"

/**
 * main - for check a code.
 *
 * Return: always zero.
 */
int main(void)
{
	print_all("ciesfs", 'B', 34, "stSchool", 1.236, NULL);
	print_all("fcsi", 3.14435, 'H', "#Cisfun", 0);
	print_all(NULL);
	return (0);
}
