#include "3-calc.h"

/**
 * op_add - addation two numbers.
 * @a: a first number.
 * @b: a second Number.
 * Return: the resulte of addation or 0.
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - subtatuse two numbers.
 * @a: a first number.
 * @b: a second Number.
 * Return: the resulte of addation or 0.
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multimal two numbers.
 * @a: a first number.
 * @b: a second Number.
 * Return: the resulte of addation or 0.
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - dividing two numbers.
 * @a: a first number.
 * @b: a second Number.
 * Return: the resulte of addation or 0.
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - get a remainder of dividing two numbers.
 * @a: a first number.
 * @b: a second Number.
 * Return: the resulte of addation or 0.
 */
int op_mod(int a, int b)
{
	return (a % b);
}
